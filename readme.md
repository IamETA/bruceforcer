# Bruceforcer 1.0
  By Erik Alvarez / aka Qdo

# Setup
 * Clone repository
 * make

# Run
 ./bruceforcer -h '<hash with salt>'

#optional parameters
  -s -> skip directory
  -b <N bytes> -> max bytesize
  -t <N threads> -> set threads
